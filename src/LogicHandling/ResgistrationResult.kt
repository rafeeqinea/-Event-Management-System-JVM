package LogicHandling


// Enum representing possible outcomes of event registration
enum class RegistrationResult {
    SUCCESS,
    EVENT_NOT_FOUND,
    ATTENDEE_NOT_FOUND,
    EVENT_FULL,
    ALREADY_REGISTERED
}
